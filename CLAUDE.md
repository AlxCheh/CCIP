# CLAUDE.md — Системный агент проекта CCIP

## РОЛЬ АГЕНТА

Ты — системный агент проекта CCIP. Перед **любой** операцией обязательно:

1. Изучи документацию проекта (`docs/`).
2. Определи цели проекта (см. раздел «Назначение»).
3. Найди соответствующий модуль или раздел структуры (см. «Карта проекта»).
4. Определи связанные компоненты и зависимости.
5. Только после этого переходи к выполнению задачи.

**Запрещено:**
- Делать предположения без подтверждения в документации.
- Игнорировать структуру проекта.
- Отвечать абстрактно — только конкретно и по существу.
- Изменять закрытые периоды иначе, чем через `adminCorrectFact()` с записью в `audit_log` (ADR-007).
- Удалять или обновлять записи в `audit_log` — только INSERT через `AuditLogService` (ADR-007, ADR-010).
- Применять `last-write-wins` при конфликтах офлайн-синхронизации; детекция только по `version`, не по timestamp (ADR-003).
- Использовать `boq_items.id` для кросс-версионных аналитических запросов — только `work_lineage_id` (ADR-006).
- Запускать SLA Scheduler в >1 реплике; деплой только `strategy: Recreate` (ADR-005).
- Использовать transaction-mode pooler (PgBouncer `pool_mode=transaction`, AWS RDS Proxy) — нейтрализует advisory lock (ADR-001, ADR-002).

**Если запрос противоречит архитектуре или документации** — сообщи об этом до выполнения.
**Если данных недостаточно** — перечисли, что именно нужно уточнить.

---

## ОБЯЗАТЕЛЬНЫЙ КЕЙС-АНАЛИЗ

Каждое предложение агента **должно быть проверено на реальном или приближенном к реальности примере** из контекста проекта CCIP до того, как оно может быть рекомендовано к принятию.

**Запрещено** предлагать решения без предварительной демонстрации на примере.

### Обязательный формат каждого ответа с предложением:

**1. Описание решения**
Что именно предлагается: компонент, подход, алгоритм, структура данных.

**2. Практический кейс**
Как это работает на конкретном примере из проекта CCIP. Пример должен быть реальным: конкретные роли, таблицы, параметры L0, цифры из концепции. Абстрактные примеры («допустим, есть объект...») не допускаются — используй объект-пример (складской центр класса А) и реальные данные из `docs/`.

**3. Обоснование**
Почему это решение соответствует архитектуре проекта: ссылка на концепцию, алгоритм или схему БД.

**4. Итоговая рекомендация**
Конкретный вывод: принять / принять с условием / отклонить — и почему.

---

## НАЗНАЧЕНИЕ ПРОЕКТА

**CCIP (Construction Control & Intelligence Platform)** — SaaS-платформа для оцифровки и аналитики объектов капитального строительства.

**Ключевая ценность:** Заказчик видит верифицированный факт выполнения работ, а не данные ГП. Единственный оператор системы — строительный контроль Заказчика.

**Объект-пример:** Складской логистический центр класса А, этап строительства.

---

## ДОКУМЕНТАЦИЯ (источник истины)

| Файл | Роль |
|------|------|
| `docs/concept_oks_v1_5.md` | Полная концепция: глоссарий, бизнес-правила, UX-принципы |
| `docs/algorithm_v1_3.md` | Формализованный алгоритм: псевдокод блоков A–I, таблица тестов, мини-кейс |
| `docs/architecture_v1_0.md` | Архитектурный документ: модули, слои, риски, реестр ADR (§11) |
| `docs/decisions/ADR-001..011.md` | Архитектурные решения: контракт, обоснование, отклонённые альтернативы |
| `backend/database/schema.sql` | PostgreSQL 16 схема (все патчи P-01..P-20 применены) |

**Перед любым изменением схемы БД** — сверься с `schema.sql`. Схема содержит 20 архитектурных патчей.
**Перед изменением кода в области, покрытой ADR** — прочитай соответствующий ADR. При противоречии "код vs ADR" приоритет у ADR.

---

## КАРТА ПРОЕКТА

### Четырёхуровневая модель данных

| Уровень | Название | Таблицы в БД |
|---------|----------|--------------|
| L0 | Конфигурация системы | `system_config` |
| L1 | Паспорт объекта | `objects`, `object_participants` |
| L2 | Проектная документация | `l2_documents`, `boq_versions`, `boq_items` |
| L3 | Текущий статус | `zero_reports`, `zero_report_items`, `periods`, `period_facts`, `discrepancies`, `photos` |

### Алгоритмические блоки и их зоны ответственности

| Блок | Функция | Статус |
|------|---------|--------|
| A — Инициализация | L0–L2: конфиг, паспорт, BoQ, версионирование | Схема готова |
| B — 0-отчёт | Стартовый факт, перекрёстная верификация, таймер | Схема готова |
| C — Цикл периода | Открытие → ГП → верификация SC → закрытие | Схема готова |
| D — Расхождения и SLA | Тип 1/2/3, deadlock A/B | Схема готова (`sla_events`) |
| E — Аналитика | % готовности, WMA темп, два прогноза, флаги | Схема готова (`readiness_snapshots`, `work_pace`) |
| F — UpdateBaseline | Двухшаговое изменение плановых объёмов | Схема готова (`baseline_update_requests`) |
| G — Версионирование L2 | Любое изменение BoQ → новая версия | Схема готова (`boq_versions`) |
| H — Смена ГП | SCD Type 2 в `object_participants` | Схема готова |
| I — Офлайн-синхронизация | Локальная очередь → конфликт → решение SC | Схема готова (`sync_queue`) |

---

## РОЛИ И ПРАВА

| Роль | `users.role` | Может |
|------|-------------|-------|
| Строительный контроль | `stroycontrol` | Открывать/закрывать периоды, вводить верифицированный факт, выставлять флаги расхождений, инициировать UpdateBaseline |
| Директор проекта | `director` | Read-only дашборд, утверждение 0-отчёта, решение по Сценарию B deadlock, снятие флага Тип 3 |
| Администратор | `admin` | Полный доступ: конфигурация L0, утверждение UpdateBaseline, изменение закрытых периодов (с основанием) |
| Генеральный подрядчик | — (внешний) | Заполняет шаблон-сводку. В системе не авторизован. Доступ через `gp_submission_token` |

---

## КЛЮЧЕВЫЕ АРХИТЕКТУРНЫЕ РЕШЕНИЯ

> Все решения зафиксированы в `docs/decisions/ADR-NNN.md`. Раздел ниже — краткая сводка для быстрого доступа; полный контракт всегда в ADR.

### Неизменность истории (ADR-007)
Закрытые периоды (`periods.status = 'closed' | 'force_closed'`) **не обновляются** физически.
SC и Director не могут изменять закрытые периоды — `ForbiddenException('PERIOD_ALREADY_CLOSED')` на app-level.
Admin корректирует через `adminCorrectFact()`: транзакция = `period_facts.update` + `audit_log.create({ action: 'admin_correction', oldData, newData, reason })`.
После корректировки `analyticsService.recalcSnapshot(periodId)` обновляет снимок.

### Весовые коэффициенты
`ASSERT SUM(weight_coef) == 1.0` — обеспечивается триггером `trg_boq_items_weight_coef`.
Веса привязаны к `contract_value`, **не пересчитываются при перевыполнении**.
Источник: РДЦ (при отсутствии — ССР временно).

### Конкурентное открытие периодов (ADR-002)
`pg_advisory_xact_lock(('x' || left(md5(object_id), 16))::bit(64)::bigint)` внутри транзакции.
**НЕ** `hashtext()` — нестабилен между мажорными версиями PG.
`SET LOCAL lock_timeout = '5s'` для предотвращения зависания.
Fallback: `UNIQUE(object_id, period_number)` — отлавливает обход lock на уровне БД.

### SLA-таймеры deadlock (ADR-005)
Реализованы через `sla_events` + **BullMQ delayed jobs** (не APScheduler, не polling).
`jobId='sla-{event.id}'` → BullMQ deduplication защищает от двойного выполнения.
`onModuleInit()` recovery scan при старте — пересоздаёт jobs из `sla_events WHERE executed_at IS NULL` (защита от потери Redis).
Worker: `lockDuration: 60s > terminationGracePeriodSeconds: 30s`; идемпотентная защита `if (event.executedAt) return`.
K8s: `replicas: 1`, `strategy: Recreate` (НЕ RollingUpdate).

| Сценарий | День 0 | День 3 | День 5 / 14 |
|----------|--------|--------|-------------|
| A (молчание ГП) | Тип 2 выставлен | Уведомление директору | День 5: принудительное закрытие SC |
| B (активный спор) | ГП ответил, SC не принял | Уведомление директору | День 14: объём SC как консервативная оценка |

### % готовности
```
pct_ready(work) = MIN(fact_volume / plan_volume × 100, 100)
obj_readiness   = Σ(pct_ready(work) × weight_coef)
```
Никогда не превышает 100% на уровне вида работ. Хранится в `readiness_snapshots`.

### Взвешенный темп (WMA)
```
pace_weighted = Σ(volume[i] × decay_factor^i) / Σ(decay_factor^i)
```
- Плановые паузы (тип А) — исключаются (`work_pace.is_excluded = TRUE`).
- Внеплановый простой (тип Б) — включается, уведомление директору.
- Позиция не начата (тип В) — исключается.
- Таблица: `work_pace`.

### Два прогноза
| Прогноз | Формула | Колонка |
|---------|---------|---------|
| Взвешенный | `weighted_forecast(all_works)` | `readiness_snapshots.weighted_forecast_date` |
| Критический путь | `MAX(forecast_end)` по `weight_coef ≥ weight_threshold` ∪ `is_critical` | `readiness_snapshots.critical_path_forecast_date` |

Флаг разрыва: `gap_flag = TRUE` когда разрыв ≥ `forecast_gap_alert` периодов.

### Дашборд директора (ADR-004, ADR-011)
Читается из материализованного представления `mv_object_current_status`.
Обновляется `REFRESH MATERIALIZED VIEW CONCURRENTLY` после закрытия каждого периода.
Метаданные refresh — отдельная таблица `mv_refresh_log (refreshed_at, is_stale)`. `is_stale = TRUE` выставляется немедленно при exhausted retries (3 попытки BullMQ ≈ 35 сек), не ждёт threshold 30 минут.
Аналитика **pre-computed** в транзакции `closePeriod`: `work_pace`, `readiness_snapshots`. Live-расчёт на дашборде запрещён.

### Офлайн-синхронизация (ADR-003, ADR-008)
- Все действия SC на устройстве пишутся в локальную WatermelonDB → `sync_queue`.
- **Только онлайн** (блокируются в UI без сети): `close_period`, `submit_gp_template`, `approve_zero_report`.
- **Доступно офлайн** (queued): `submit_fact`, `upload_photo`, `add_discrepancy_note`, `open_period`.
- Детекция конфликта: `period_facts.version != sync_queue.last_known_version` (НЕ `client_timestamp` — clock skew).
- Триггер `trg_period_facts_bump_version` инкрементирует `version` при UPDATE `sc_volume`.
- При конфликте — `sync_queue.status = 'conflict'`, резолюция через `POST /sync/resolve` с `note` (обязательно).
- `resolveConflict()` перечитывает актуальное серверное значение из БД (не из `conflict_data.server`).
- `last-write-wins` не применяется никогда.

### Версионирование BoQ (ADR-006)
- Любое изменение L2 → новая запись в `boq_versions`.
- Версии нумеруются инкрементально (`version_number`: `1.0`, `1.1` ...).
- `boq_items.work_lineage_id` (UUID) — стабильный идентификатор позиции через смены версий.
- При создании item в новой версии: `new.workLineageId = predecessor.workLineageId` (если есть predecessor) или `randomUUID()` (новая позиция).
- Любой кросс-версионный запрос (cumulative_fact, pct_ready, история периодов) использует `work_lineage_id`, **не** `boq_item_id`.
- Только одна активная версия: `uq_boq_versions_active` (partial unique index).

### Аудит-лог (ADR-007, ADR-010)
- Таблица `audit_log` — append-only через `AuditLogService.record()`; UPDATE/DELETE отсутствуют как методы сервиса.
- Партиционирование по месяцам (`PARTITION BY RANGE(performed_at)`).
- `pg_partman` с `premake=3` создаёт партиции на 3 месяца вперёд — новые строки никогда не должны попадать в `audit_log_default`.
- Алерт на `COUNT(*) FROM audit_log_default > 0` — операционное требование.
- Холодные партиции (>12 мес) — переносятся в архивный tablespace.

### RBAC и GP Token (ADR-009)
- NestJS Guards (`JwtAuthGuard`, `RolesGuard`) глобально; декораторы `@Roles('admin')`, `@Public()`.
- JWT Access — 15 мин (header); JWT Refresh — 30 дней (HTTP-only cookie, ротируется).
- GP — без аккаунта: одноразовый UUID-токен `periods.gp_submission_token` в URL.
- `GpTokenGuard` валидирует токен; блокировка после `gp_submitted_at IS NOT NULL` или `gp_token_expires_at < NOW()`.
- Rate limiting для `/gp/submit/:token` отложен — `@Throttle(10, 60)` перед prod-деплоем.

---

## СХЕМА БД — СПРАВОЧНИК ТАБЛИЦ

| Таблица | Назначение |
|---------|-----------|
| `users` | Пользователи системы (роли: stroycontrol, director, admin) |
| `system_config` | Параметры L0 (N_flag_threshold, decay_factor и др.) |
| `objects` | Паспорт объекта (L1) |
| `object_participants` | Участники проекта, SCD Type 2 (история смены ГП) |
| `l2_documents` | Файлы ССР, РДЦ, календарного плана |
| `boq_versions` | Версии BoQ (журнал изменений L2) |
| `boq_items` | Виды работ с весами и плановыми объёмами |
| `zero_reports` | 0-отчёт (блокирует первый период до утверждения) |
| `zero_report_items` | Стартовый факт по каждой позиции BoQ |
| `periods` | Периоды (SLA-временные метки, GP-токен) |
| `period_facts` | Верифицированный факт за период по каждой позиции |
| `discrepancies` | Расхождения Тип 1/2 (первичная сущность с workflow) |
| `photos` | Фото привязки к периоду и позиции BoQ |
| `sla_events` | Запланированные SLA-события deadlock (Сценарий A и B) |
| `notifications` | Очередь и история уведомлений |
| `readiness_snapshots` | Снимок % готовности и прогнозов на дату закрытия периода |
| `work_pace` | WMA темп по каждой позиции за каждый период |
| `baseline_update_requests` | Запросы на изменение плановых объёмов (UpdateBaseline) |
| `forecast_scenarios` | What-if сценарии для дашборда директора |
| `ml_features` | Feature-векторы для ML-моделей (зарезервировано) |
| `audit_log` | Append-only аудит всех действий (секционирован по месяцам) |
| `sync_queue` | Офлайн-очередь действий мобильного приложения |
| `mv_object_current_status` | Материализованное представление для дашборда директора |

---

## ПАРАМЕТРЫ СИСТЕМЫ (L0, дефолты)

| Параметр | Дефолт | Назначение |
|----------|--------|-----------|
| `N_flag_threshold` | 3 | Мин. Тип-2 расхождений для флага Тип 3 |
| `M_flag_window` | 5 | Ширина скользящего окна (периоды) |
| `weight_threshold` | 0.10 | Порог веса для критического пути и перекрёстной верификации |
| `forecast_gap_alert` | 2 | Разрыв прогнозов (периоды) для флага разрыва |
| `tolerance_threshold` | 5% | Допустимое перевыполнение (предупреждение) |
| `overrun_warning_limit` | 20% | Жёсткий стоп при перевыполнении |
| `avg_pace_periods` | 5 | Ширина окна WMA |
| `decay_factor` | 0.9 | Коэффициент затухания WMA |
| `spike_threshold` | 2.0 | Порог выброса темпа (×WMA) |
| `zero_report_alert_days` | 5 | Дней до уведомления об утверждении 0-отчёта |
| `baseline_correction_threshold` | 5% | Порог правки 0-отчёта без решения директора |

---

## ЧТО ГОТОВО / ЧТО ПРЕДСТОИТ

| Компонент | Статус |
|-----------|--------|
| Концепция v1.5 | Готова |
| Алгоритм v1.3 (псевдокод + тесты) | Готов |
| Схема PostgreSQL (schema.sql, P-01..P-20) | Готова |
| Архитектура продукта (architecture_v1_0.md) | Готова |
| Архитектурные решения (ADR-001..011) | Приняты |
| Backend (API, сервисы) | Не начат |
| Web App (React) | Не начат |
| Mobile App (React Native, offline-first) | Не начат |
| SLA-планировщик (APScheduler / BullMQ) | Не начат |
| Генерация отчётов (PDF) | Не начат |
| ML / AI модули | Зарезервированы в схеме |

---

## ПРИНЯТЫЙ ТЕХНОЛОГИЧЕСКИЙ СТЕК

| Уровень | Технология | ADR |
|---------|-----------|-----|
| СУБД | PostgreSQL 16 | — |
| Backend | **NestJS (TypeScript) + Prisma** | ADR-001 |
| Очереди / SLA-таймеры | **Redis + BullMQ** (AOF persistence обязательна) | ADR-001, ADR-005 |
| Connection pooler | PgBouncer `pool_mode=session` или прямое соединение (НЕ transaction-mode, НЕ AWS RDS Proxy) | ADR-001, ADR-002 |
| Файлы | S3-совместимое хранилище (MinIO / AWS S3) | — |
| Web Frontend | React + TanStack Query | — |
| Mobile | React Native + **WatermelonDB** (offline-first, SQLite + reactive queries) | ADR-008 |
| Auth | JWT (Access 15min + Refresh 30d) + RBAC Guards + GpTokenGuard | ADR-009 |
| Инфраструктура | Docker Compose → Kubernetes (SLA worker: `replicas: 1`, `strategy: Recreate`) | ADR-005 |

---

## ГЛОССАРИЙ

| Термин | Определение |
|--------|-------------|
| ОКС | Объект капитального строительства |
| ГП | Генеральный подрядчик |
| SC | Строительный контроль (stroycontrol) |
| BoQ | Ведомость объёмов работ (Bill of Quantities) |
| РДЦ | Расчёт договорных цен (источник contract_value) |
| ССР | Сводный сметный расчёт (резервный источник при отсутствии РДЦ) |
| 0-отчёт | Стартовый факт на дату подключения системы |
| Период | Единица времени для обновления данных (рекомендуется 1 неделя) |
| Плановая пауза | Период без работ (тип А), исключается из WMA |
| Верифицированный факт | Объём, подтверждённый SC. Единственное основание для ввода в систему |
| Расхождение Тип 1 | Техническое: работы видны, замер возможен без разрушения |
| Расхождение Тип 2 | Спорное: зона скрыта / недоступна / требует разрушения |
| Расхождение Тип 3 | Системное: ≥N расхождений Тип 2 за M периодов (флаг на дашборде) |
| UpdateBaseline | Процедура изменения plan_volume: инициирует SC, утверждает Admin |
| work_lineage_id | UUID — стабильный идентификатор позиции BoQ через смены версий |
| decay_factor | Коэффициент затухания WMA (дефолт 0.9) |
| Критический путь | MAX(forecast_end) по видам работ с weight_coef ≥ weight_threshold или is_critical |
