apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# TODO: cloud provider selection — separate follow-up plan
# (managed Postgres/Redis vs StatefulSet, real ingress class, storageClass)
