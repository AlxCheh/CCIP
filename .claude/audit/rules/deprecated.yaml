# deprecated.yaml — архив отвергнутых/устаревших правил с причинами.
# Правило попадает сюда при: hit_count==0 за 20 сессий ИЛИ precision<0.4
# ИЛИ удаление critical context (без права восстановления).
# Не удалять записи — это история обучения. См. ADR-016.

version: 1
deprecated: []
