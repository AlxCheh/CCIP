# Used by prisma migrate / prisma db seed
# Points directly to Postgres (NOT PgBouncer) — advisory locks require session mode
DATABASE_URL="postgresql://ccip_owner:ccip_dev_pass@localhost:5432/ccip?schema=public"
